import 'package:flutter/material.dart';

/// Widget: Preview datos del extintor
class ExtinguisherDataPreview extends StatelessWidget {
  const ExtinguisherDataPreview({super.key});

  @override
  Widget build(BuildContext context) {
    return const Card(
      child: Padding(
        padding: EdgeInsets.all(16),
        child: Text('Extinguisher Data Preview - TODO'),
      ),
    );
  }
}

