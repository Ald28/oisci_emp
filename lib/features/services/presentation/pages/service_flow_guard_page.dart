import 'package:flutter/material.dart';

/// Pantalla: Decide nuevo? -> datos o registro
class ServiceFlowGuardPage extends StatelessWidget {
  const ServiceFlowGuardPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Service Flow Guard Page - TODO'),
      ),
    );
  }
}

