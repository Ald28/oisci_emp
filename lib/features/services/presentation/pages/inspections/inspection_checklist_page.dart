import 'package:flutter/material.dart';

/// Pantalla: Checklist inspección
class InspectionChecklistPage extends StatelessWidget {
  const InspectionChecklistPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Inspection Checklist Page - TODO'),
      ),
    );
  }
}

