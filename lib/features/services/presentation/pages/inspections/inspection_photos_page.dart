import 'package:flutter/material.dart';

/// Pantalla: Fotos 1/2/3 inspección
class InspectionPhotosPage extends StatelessWidget {
  const InspectionPhotosPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Inspection Photos Page - TODO'),
      ),
    );
  }
}

