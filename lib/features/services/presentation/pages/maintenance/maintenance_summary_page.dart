import 'package:flutter/material.dart';

/// Pantalla: Resumen + Finalizar mantenimiento
class MaintenanceSummaryPage extends StatelessWidget {
  const MaintenanceSummaryPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Maintenance Summary Page - TODO'),
      ),
    );
  }
}

