import 'package:flutter/material.dart';

/// Pantalla: Input de ítem (ej: cartucho)
class MaintenanceItemInputPage extends StatelessWidget {
  const MaintenanceItemInputPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Maintenance Item Input Page - TODO'),
      ),
    );
  }
}

